# Cargo build output.
/target/

# flutter_rust_bridge codegen output. Generated by `just gen` / the codegen
# command in README.md; regenerate rather than commit, so it can never drift
# from the Rust signatures it mirrors.
/src/frb_generated.rs
